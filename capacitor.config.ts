import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.googlesheets.starter',
  appName: 'Ionic React GoogleSheets Starter',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
